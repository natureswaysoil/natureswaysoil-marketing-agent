import type { CampaignRecord } from './types.js'

const esc = (value: unknown) => String(value ?? '').replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char]!))

export function renderDashboard(records: CampaignRecord[], token: string): string {
  const cards = records.map(item => `<article><h2>${esc(item.hook)}</h2><p><strong>${esc(item.productId)}</strong> · ${esc(item.objective)} · ${esc(item.status)}</p><p>${esc(item.angle)}</p><details><summary>Video and captions</summary><p>${esc(item.videoBrief.voiceover)}</p>${item.posts.map(p => `<h3>${esc(p.channel)}</h3><p>${esc(p.caption)}</p>`).join('')}</details>${item.status === 'pending_approval' ? `<form method="post" action="/campaigns/${esc(item.id)}/approve"><input type="hidden" name="token" value="${esc(token)}"><button>Approve & send to video</button></form><form method="post" action="/campaigns/${esc(item.id)}/reject"><input type="hidden" name="token" value="${esc(token)}"><button class="secondary">Reject</button></form>` : ''}</article>`).join('')
  return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Nature's Way Soil Marketing Agent</title><style>body{font:16px system-ui;max-width:960px;margin:auto;padding:24px;background:#f5f2e8;color:#173d2a}article{background:white;border:1px solid #d5dfd4;border-radius:14px;padding:20px;margin:16px 0}button{background:#23633f;color:white;border:0;border-radius:8px;padding:10px 14px;margin:8px 8px 0 0}.secondary{background:#6b5b4b}form{display:inline}summary{cursor:pointer}h1{margin-bottom:4px}</style></head><body><h1>Nature's Way Soil Marketing Agent</h1><p>Approval-first campaign queue. Nothing publishes until you approve it.</p>${cards || '<p>No campaigns yet.</p>'}</body></html>`
}
